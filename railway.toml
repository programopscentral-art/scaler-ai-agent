[build]
builder = "nixpacks"
buildCommand = "python3 -m pip install -r backend/requirements.txt && cd frontend && npm install && npm run build"

[deploy]
startCommand = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
